<!-- Twtitter -->
<% if @user.social_medium.twitter != "" %>
  <%= form_for(@social) do |f| %>
    <%= f.submit "X",
      data: { confirm: 'Are you sure you want to delete',
              'confirm-button-text': 'Yea',
              'cancel-button-text': 'Nay',
              'confirm-button-color': '#a5d6a7',
              'cancel-button-color': '#e57373',
              'sweet-alert-type': 'error',
              text: "@#{@user.social_medium.twitter}"}
    %>
    <%= f.label "#{:twitter} @#{@user.social_medium.twitter}" %>
    <%= f.text_field :twitter, style: 'display: none;', value: '' %>
  <% end %>
<% end %>

<!-- Github -->
<% if @user.social_medium.github != "" %>
  <%= form_for(@social) do |f| %>
    <%= f.submit "X",
      data: { confirm: 'Are you sure you want to delete',
              'confirm-button-text': 'Yea',
              'cancel-button-text': 'Nay',
              'confirm-button-color': '#a5d6a7',
              'cancel-button-color': '#e57373',
              'sweet-alert-type': 'error',
              text: "@#{@user.social_medium.github}"}
    %>
    <%= f.label "#{:github} @#{@user.social_medium.github}" %>
    <%= f.text_field :github, style: 'display: none;', value: '' %>
  <% end %>
<% end %>

<!-- Instagram -->
<% if @user.social_medium.instagram != "" %>
  <%= form_for(@social) do |f| %>
    <%= f.submit "X",
      data: { confirm: 'Are you sure you want to delete',
              'confirm-button-text': 'Yea',
              'cancel-button-text': 'Nay',
              'confirm-button-color': '#a5d6a7',
              'cancel-button-color': '#e57373',
              'sweet-alert-type': 'error',
              text: "@#{@user.social_medium.instagram}"}
    %>
    <%= f.label "#{:instagram} @#{@user.social_medium.instagram}" %>
    <%= f.text_field :instagram, style: 'display: none;', value: '' %>
  <% end %>
<% end %>

<!-- Youtube -->
<% if @user.social_medium.youtube != "" %>
  <%= form_for(@social) do |f| %>
    <%= f.submit "X",
      data: { confirm: 'Are you sure you want to delete',
              'confirm-button-text': 'Yea',
              'cancel-button-text': 'Nay',
              'confirm-button-color': '#a5d6a7',
              'cancel-button-color': '#e57373',
              'sweet-alert-type': 'error',
              text: "@#{@user.social_medium.youtube}"}
    %>
    <%= f.label "#{:youtube} @#{@user.social_medium.youtube}" %>
    <%= f.text_field :youtube, style: 'display: none;', value: '' %>
  <% end %>
<% end %>

<!-- Facebook -->
<% if @user.social_medium.facebook != "" %>
  <%= form_for(@social) do |f| %>
    <%= f.submit "X",
      data: { confirm: 'Are you sure you want to delete',
              'confirm-button-text': 'Yea',
              'cancel-button-text': 'Nay',
              'confirm-button-color': '#a5d6a7',
              'cancel-button-color': '#e57373',
              'sweet-alert-type': 'error',
              text: "@#{@user.social_medium.facebook}"}
    %>
    <%= f.label "#{:facebook} @#{@user.social_medium.facebook}" %>
    <%= f.text_field :facebook, style: 'display: none;', value: '' %>
  <% end %>
<% end %>

<%= link_to 'Add new', edit_social_medium_url %>
